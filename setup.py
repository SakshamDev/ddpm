from setuptools import setup, find_packages

setup(
    name="ddpm",
    version="0.1.0",
    description="DDPM from scratch on CIFAR-10",
    packages=find_packages(),
    python_requires=">=3.9",
)
